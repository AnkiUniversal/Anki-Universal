<UserControl
    x:Class="AnkiU.UserControls.NotificationPopup"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AnkiU.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="userControl"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Popup x:Name="popup" VerticalAlignment="Top" MaxHeight="300" HorizontalAlignment="Stretch"
                VerticalOffset="10" HorizontalOffset="0" MaxWidth="300" >
        <Grid                
                BorderBrush="{Binding Foreground, ElementName=userControl}" HorizontalAlignment="Center"
                Background="{StaticResource DefaultAcrylicBrush}" VerticalAlignment="Center"
                BorderThickness="2" MaxWidth="300" MaxHeight="300">
            <TextBlock Margin="10" x:Name="textBlock" TextWrapping="Wrap"  TextAlignment="Center"
                       FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </Popup>
</UserControl>
